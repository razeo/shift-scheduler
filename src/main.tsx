import React from 'react';
import ReactDOM from 'react-dom/client';
import { RestoHubApp } from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RestoHubApp />
  </React.StrictMode>,
);
